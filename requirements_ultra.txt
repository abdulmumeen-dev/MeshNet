# MeshNet Ultra Dependencies
# Install with: pip install -r requirements_ultra.txt

# Required
cryptography>=41.0.0
numpy>=1.24.0

# Optional - for better performance
requests>=2.28.0
psutil>=5.9.0
pywin32>=305; sys_platform == 'win32'

# For GUI (optional)
tkinter  # Comes with Python

# For mobile (Termux)
# All dependencies work on Android
